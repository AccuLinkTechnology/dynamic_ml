'''
CONFIG
pip3 install --index-url https://pypi.org/simple --extra-index-url https://pypi.ngc.nvidia.com pandas

Docker:
sudo docker run --rm -it --runtime nvidia --network host   -v /home/nvidia/Documents/dynamic_ml:/workspace/dynamic_ml   dustynv/l4t-pytorch:r36.4.0
'''

python3 - <<'PY'
import torch
p="runs_ref/20260129_183328_ref/label_stats.pt"
d=torch.load(p, map_location="cpu")
print("label_norm:", d["label_norm"])
print("img_size:", d["img_size"])
print("mode:", d["mode"])
print("mean:", d["global"]["mean"].tolist())
print("std :", d["global"]["std"].tolist())
PY
