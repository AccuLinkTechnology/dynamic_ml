1) DO wobbulator sequence generation
    -> Get a demo known sequence: no pics
    -> Get ground truth CSV file for sequence (dynamic)

Practice 2 on non-demo lighting too.
2) Run the same demo sequence, and see the results of inference on it (demo lighting)
    -> Move to all positions.
    -> Stop at each position, click 'Enter' on infer_live to log calculated deltas
    -> Compare jetson's inference to ground truth CSV.

3) Depending on results, create a tailored 'smoother'/PID
    -> Takes Jetson inference, runs PID, generates similar movement to ground truth.
    -> Try not to over-generalise with PID
    -> This inference is sent over UDP to PI. 
    -> Jetson command should? be stable, don't just blast the pi with numbers like currently
    -> issue a 'best command' every 5s: reflects wob movement.
    -> PI clamping: no movement < 7.5 and > 12.5 with our seq numbers

4) Demo Ready:
    -> Lower speed on twist motor.
    -> Make it continious?
    -> Make it loop?

5) Clean directories: keep only w